"""Travel Intelligence: a small, defensible trip-recommendation system.

Deterministic Python owns the business logic (retrieval, features, ranking, constraints,
budget). The LLM is one component of the system, not the system.
"""

__version__ = "0.1.1"
